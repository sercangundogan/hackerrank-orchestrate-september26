[pytest]
pythonpath = code
testpaths = tests
# The challenge entry point lives in a directory named `code/`, which would
# otherwise shadow the stdlib `code` module imported by pytest's debugging plugin.
addopts = -p no:debugging
